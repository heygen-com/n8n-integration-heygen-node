import type { INodeProperties } from 'n8n-workflow';

/** Video ID for Get / Delete Video lives in `videos_v3.desc.ts`; keep export for backwards-compatible barrel. */
export const getVideoStatus: INodeProperties[] = [];
